+++
date = '2026-09-19'
draft = false
title = 'Java 八股自测问题'
tags = ["JVM", "JUC", "八股"]
categories = ["Java"]
+++

> 最后更新：2026-09-19

# JVM

## 内存模型

1. JVM 的内存模型介绍一下？
2. JVM 内存模型里的堆和栈有什么区别？
3. 栈中存的到底是指针还是对象？
4. 堆分为哪几部分？
5. 如果有个大对象，一般是在哪个区域？
6. 程序计数器的作用是什么？为什么是私有的？
7. 方法区中的方法的执行过程是怎样的？
8. 方法区中还有哪些东西？
9. `String` 保存在哪里？
10. `String s = new String("abc")` 执行过程中分别对应哪些内存区域？
11. 引用类型有哪些？有什么区别？
12. 弱引用了解吗？举例说明在哪里可以使用？
13. 如何理解内存泄漏和内存溢出？
14. JVM 内存结构有哪几种内存溢出的情况？
15. 遇到过堆溢出的情况吗？如何解决？
16. 栈溢出的情况呢？
17. 有具体的内存泄漏和内存溢出的例子吗？请举例并说明解决方案？

## 类初始化和类加载

1. 创建对象的过程是怎样的？
2. 对象的生命周期是怎样的？
3. 类加载器有哪些？
4. Java 中的双亲委派是什么？有什么用？
5. 双亲委派模型的作用是什么？
6. 类加载过程是怎样的？
7. 讲一下类的加载和双亲委派原理？

## 垃圾回收

1. 什么是 Java 里的垃圾回收？如何触发垃圾回收？
2. 判断垃圾的方法有哪些？
3. 垃圾回收算法是什么？是为了解决什么问题？
4. Java 的 GC 机制你知道哪些？
5. 垃圾回收算法有哪些？
6. 垃圾回收器有哪些？
7. 标记-清除算法的缺点是什么？
8. 垃圾回收算法的哪些阶段会发生 Stop-the-World？
9. Minor GC、Major GC、Full GC 的区别是什么？什么场景会触发 Full GC？
10. CMS 和 G1 垃圾回收器的区别是什么？
11. 什么情况下使用 CMS？什么情况下使用 G1？
12. G1 回收器的特色是什么？
13. GC 只会对堆进行回收吗？

# JUC

## 多线程

1. Java 的内存模型（JMM）介绍一下？
2. Java 多线程是什么？需要注意什么？
3. Java 里的线程和操作系统的线程一样吗？
4. 使用多线程要注意哪些问题？
5. 保证数据的一致性有哪些方案？
6. 线程的创建方式有哪些？
7. 怎么启动线程？
8. 如何停止一个线程的运行？
9. 调用 `interrupt()` 是如何让线程抛出异常的？
10. Java 线程的状态有哪些？
11. `sleep()` 和 `wait()` 的区别是什么？
12. `sleep()` 会释放 CPU 吗？
13. `BLOCKED` 和 `WAITING` 有什么区别？
14. `WAITING` 状态下的线程如何恢复到 `RUNNABLE` 状态？
15. `notify()` 和 `notifyAll()` 的区别？
16. `notify()` 选择哪个线程？
17. 不同的线程之间如何通信？
18. 线程间的通信方式有哪些？
19. 如何停止一个线程？
20. Go 的协程和 Java 的线程有什么区别？

## 并发安全

1. JUC 包下你常用的类有哪些？
2. 怎么保证多线程安全？
3. Java 中有哪些常用的锁？分别在什么场景下使用？
4. 实践中怎么使用锁？
5. Java 并发工具你知道哪些？
6. `CountDownLatch` 是做什么的？讲一讲？
7. `synchronized` 和 `ReentrantLock` 分别适用于什么场景？
8. 除了使用 `synchronized`，还有什么方法可以实现线程同步？
9. `synchronized` 锁静态方法和普通方法有什么区别？
10. `synchronized` 和 `ReentrantLock` 有什么区别？
11. 怎么理解可重入锁？
12. `synchronized` 支持重入吗？如何实现的？
13. `synchronized` 锁升级的过程讲一下？
14. JVM 对 `synchronized` 做了哪些优化？
15. 介绍一下 AQS？
16. CAS 和 AQS 有什么关系？
17. 如何用 AQS 实现一个可重入的公平锁？
18. `ThreadLocal` 的作用和原理是什么？其中存储的 key-value 是什么？会有什么问题？如何解决？
19. 悲观锁和乐观锁的区别？
20. Java 中想实现一个乐观锁，都有哪些方式？
21. CAS 有什么缺点？
22. 为什么不能所有的锁都用 CAS？
23. CAS 有什么问题？Java 是怎么解决的？
24. `volatile` 关键字有什么作用？
25. 指令重排序的原理是什么？
26. `volatile` 可以保证线程安全吗？
27. `volatile` 和 `synchronized` 有什么区别？
28. 什么是公平锁和非公平锁？
29. 非公平锁的吞吐量为什么比公平锁大？
30. `synchronized` 是公平锁吗？
31. `ReentrantLock` 是怎么实现公平锁的？
32. 什么情况会产生死锁问题？如何解决？

## 线程池

1. 线程池怎么使用？
2. 介绍一下线程池的工作原理？
3. 线程池的参数有哪些？
4. 线程池工作队列满了有哪些拒绝策略？
5. 有线程池参数设置的经验吗？核心线程数设置为 0 可以吗？
6. 线程池种类有哪些？
7. 线程池一般是怎么用的？
8. 线程池和直接创建三个线程并发相比有什么优势？
9. 线程池用了哪些设计模式？
10. 线程池中 `shutdown()`、`shutdownNow()` 这两个方法有什么作用？
11. 提交给线程池中的任务可以被撤回吗？

## 场景题

1. 多线程场景下如何控制打印顺序？
2. 多线程打印奇偶数，怎么控制？
3. 单例模式既然已经使用了 `synchronized`，为什么还要再加 `volatile`？
4. 3 个线程并发执行，另有 1 个线程等待这 3 个线程全部执行完再执行，怎么实现？
5. 假设两个线程并发读写同一个整型变量，初始值为 0，每个线程加 50 次，结果可能是什么？
