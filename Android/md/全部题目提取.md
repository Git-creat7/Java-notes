# Android 题目全集提取

> 说明：本文件只整理题目、要求、必要代码框架和已知条件，已尽量删除混在源文件里的答案代码。
> 来源范围：根目录 `2022-2023真题.txt`、`test - *.txt`、`test -9没有让提交.txt`、根目录 `1.doc` 至 `5.doc` 实验题。
> 实验题 `.doc` 原文件含图片和二进制内容，已根据可读正文提取题目要求，图片只能保留为文字描述。

## 一、2022-2023 真题

来源：`2022-2023真题.txt`

### 1. 选择题

本大题共 10 小题，每小题 2 分，共 20 分。

1. 下列选项中，指定项目中使用的 SDK 路径的文件是（）。
A. app.iml
B. build.gradle
C. setting.gradle
D. local.properties

2. 在下列选项的（）中编写界面布局，能够有效的将界面中的布局代码和 Java 代码隔离。
A. XML 布局文件
B. Java 文件
C. gradle 文件
D. View 文件

3. 下列选项中，用于设置 CheckBox 控件的选择监听事件的方法是（）。
A. setOnClickListener()
B. setOnCheckedListener()
C. setOnCheckedChangeListener()
D. setOnMenuItemSelectedListener()

4. 下面关于任务栈的描述，正确的是（）。
A. 任务栈有压栈和出栈的操作
B. 当打开 Activity 时，该 Activity 就被压入栈底
C. 当 Activity 被销毁时，该 Activity 的实例从栈底弹出
D. 任务栈只有出栈的操作

5. 下面关于 Fragment 的描述，正确的是（）。
A. Fragment 不能在多个 Activity 中使用
B. 使用 Fragment，能使程序更加合理和充分地利用屏幕的空间
C. Fragment 可以代替 Activity 使用
D. Fragment 不能嵌入到 Activity 中

6. 下列选项中，用于存储 String 类型的数据到 SharedPreferences 文件中的方法是（）。
A. set()
B. put()
C. getString()
D. putString()

7. 下面关于 SQLite 数据库的描述中，错误的是（）。
A. SqliteOpenHelper 类有创建数据库和更新数据库版本的功能
B. 当数据库版本发生变化时，会调用 SqliteOpenHelper 的 onUpgrade() 方法更新数据库
C. 每次调用 SqliteDatabase 的 getWritableDatabase() 方法时，都会执行 SqliteOpenHelper 的 onCreate() 方法
D. 可以调用 SqliteDatabase 类的 insert() 方法在数据库中插入数据

8. 当使用 bindService() 方法开启服务时，下列选项中关于服务生命周期的描述，正确的是（）。
A. onCreate() -> onStart() -> onBind() -> onDestroy()
B. onCreate() -> onBind() -> onDestroy()
C. onCreate() -> onBind() -> onUnBind() -> onDestroy()
D. onCreate() -> onStart() -> onBind() -> onUnBind() -> onDestroy()

9. 下面关于 Bitmap 类的描述，错误的是（）。
A. createBitmap() 方法用于创建 Bitmap 类对象
B. Config 表示位图
C. 通过设置 Config 的值可以影响图片的质量和透明度
D. Bitmap.Config 表示 Bitmap 的每个像素占用的字节数

10. 下列选项中，可以将 SurfaceView 控件与 MediaPlayer 类进行关联的方法是（）。
A. setDisplay()
B. getHolder()
C. getSurfaceHolder()
D. getDisplay()

### 2. 广播

共 10 分。

已知一广播接收者的定义如下：

```java
class MyReceiver extends BroadcastReceiver{
    @Override
    public void onReceive(Context context, Intent intent) {
        Toast.makeText(context, "已接收到广播！", Toast.LENGTH_SHORT).show();
    }
}
```

1. 使用动态注册广播代码注册该广播，用来接收 action 为 `zzuli` 的广播。说明：代码在哪个方法内，写出该方法名即可。（6 分）

```java
public class ActivityA extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_a);
        
    }

    @Override
    protected void onDestroy() {
        super.onDestroy();
        
    }
}
```

2. ActivityA 的布局中有一个按钮，在布局中为其绑定了单击事件处理：`android:onClick="send"`。单击按钮将发送广播，使 MyReceiver 接收发送的广播，请写出代码。（4 分）

### 3. 网络访问

共 35 分。

已知某网站的 API 如下：

地址：`https://www.tb.com/book/list`

请求方法：POST

请求参数：`page`、`pagesize`

请使用 `HttpURLConnection` 实现数据的获取与显示功能，要求如下：

1. 发送 POST 请求（page:1，pagesize:10），获取返回流。（15 分）
2. 将流转换为字符串。（10 分）
3. ActivityB 的布局中有一个 TextView（id: `textView`），将获取的字符串显示在 TextView 中。（10 分）

```java
public class ActivityB extends AppCompatActivity {

    private Handler handler=new Handler() {
        @Override
        public void handleMessage(@NonNull Message msg) {
            
        }
    };

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_b);
        
    }
}
```

### 4. 列表显示数据

共 35 分。

已知：获取的 JSON 数据格式如图 1 所示，保存数据的 Book 类如图 2，已为其生成相应的构造函数及相应的 get、set 方法，可以直接调用。

图 1 JSON 格式：

```json
{
    "code": 200,
    "message": "成功！",
    "result": {
        "list": [
            {
                "name": "string",
                "author": "string",
                "price": 0.0,
                "link": "string"
            }
        ],
        "total": 0
    }
}
```

图 2 Book 类：

```java
public class Book {
    private String name;
    private String author;
    private double price;
    private String link;
    // 省略构造函数及相应的 get/set 方法
}
```

请按要求给出核心代码：

1. 完成 `parseBooks`，实现 JSON 字符串解析。（13 分）
2. 将适配器补充完整，Item 使用布局文件 `item_book`，包含三个 TextView，id 分别为：`tv_name`、`tv_author`、`tv_price`。（12 分）
3. ActivityC 的布局中有一个 ListView（id:`lv_book`），使用该 ListView 显示所有的图书。（5 分）
4. 完成 ListView 的单击事件处理，单击打开 BookActivity，并携带图书的 link。（5 分）

```java
public class ActivityC extends AppCompatActivity {
    private List<Book> books;
    private String str="...";

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_c);
        books=parseBooks(str);
        
        // ListView点击事件
        listView.setOnItemClickListener(new AdapterView.OnItemClickListener() {
            @Override
            public void onItemClick(AdapterView<?> parent, View view, int position, long id) {
                
            }
        });
    }

    private List<Book> parseBooks(String str) {
        
    }
}

class BookAdapter extends BaseAdapter{
    @Override
    public int getCount() {//1分
        
    }
    @Override
    public Object getItem(int position) {//1分
        
    }
    @Override
    public long getItemId(int position) {//1分
        
    }
    @Override
    public View getView(int position, View convertView, ViewGroup parent) {//9分
        
    }
}
```

## 二、作业题

### 作业 1：ListView 显示 stus

来源：`test - 1.txt`

Activity 布局内添加了一个 ListView（id: `lv1`），用来显示上题的 `stus`，请按要求完成代码。

完成适配器，用来显示 `stus`。

```java
class StuAdapter extends BaseAdapter{
    @Override
    public int getCount() {
        // 1（2分）
    }

    @Override
    public Object getItem(int position) {
        // 2（2分）
    }

    @Override
    public long getItemId(int position) {
        // 3（2分）
    }

    @Override
    public View getView(int position, View convertView, ViewGroup parent) {
        // 4 加载的布局名：stu_item，包含两个TextView，id分别为tv1和tv2（10分）
    }
}
```

请写出 ListView 设置适配器的代码。（4 分）

```java
// 5
```

### 作业 2：Toast 与 Student 初始化

来源：`test - 2.txt`

#### 1. 按钮单击事件处理

已知 Activity 的布局文件中有一个 EditText（id:`et1`）和一个按钮（id:`but1`，onClick:`show`），请完成按钮的单击事件处理：获取用户输入内容，并使用 Toast 显示用户输入。（10 分）

```java
public void show(View view) {
    
}
```

#### 2. Student 实体类与数据初始化

已知 Student 实体类定义如下：

```java
public class Student {
    private String sno;//学号
    private String sname;//姓名

    public Student() {
    }

    public Student(String sno, String sname) {
        this.sno = sno;
        this.sname = sname;
    }

    public String getSno() {
        return sno;
    }

    public void setSno(String sno) {
        this.sno = sno;
    }

    public String getSname() {
        return sname;
    }

    public void setSname(String sname) {
        this.sname = sname;
    }
}
```

在 Activity 内定义：

```java
private List<Student> stus;

private void initDatas() {
    // 往 stus 里添加10条记录

}
```

请完成 `initDatas()` 方法。（10 分）

### 作业 3：RecyclerView 显示 stus

来源：`test - 3.txt`

使用 RecyclerView 显示 `stus`。

Activity 布局内添加了一个 RecyclerView（id:`rv1`），请按要求完成代码。

完成适配器，加载的布局名：`stu_item`，包含两个 TextView，id 分别为 `tv1` 和 `tv2`。

```java
class RvAdapter extends RecyclerView.Adapter<RvAdapter.MyHolder>{
    @NonNull
    @Override
    public MyHolder onCreateViewHolder(@NonNull ViewGroup parent, int viewType) {
        // 1.（4分）
    }

    @Override
    public void onBindViewHolder(@NonNull MyHolder holder, int position) {
        // 2.（4分）
    }

    @Override
    public int getItemCount() {
        // 3.（2分）
    }

    class MyHolder extends RecyclerView.ViewHolder{
        // 4.（2分）

        public MyHolder(@NonNull View itemView) {
            super(itemView);
            // 5.（2分）
        }
    }
}
```

请写出 RecyclerView 设置适配器的代码。（6 分）

```java
// 6
```

### 作业 4：返回键对话框与 Activity 跳转

来源：`test - 4.txt`

#### 1. 返回键退出确认

请完成如下程序，按下返回键时弹出对话框，按下确定退出。（10 分）

提示：确定要退出吗？按钮：取消、确定。

```java
@Override
public void onBackPressed() {
    AlertDialog dialog;
    dialog=new AlertDialog.Builder(this)
            .         1           ("提示")
            .         2           ("确定要退出吗？")
            .         3           ("确定", new DialogInterface.OnClickListener() {
                @Override
                public void onClick(DialogInterface dialog, int which) {
                             4                 
                }
            })
            .setNegativeButton("取消",null)
            .create();
    dialog.          5                  ;
}
```

#### 2. Activity 跳转

1. ActivityA 有一个编辑框（id:`et1`），读取用户输入内容作为参数，跳转至 ActivityB，请写出代码。（5 分）
2. ActivityB 内接收数据，并显示到 TextView（id:`tv1`）。（5 分）

### 作业 5：ActivityA 打开 ActivityB 获取联系人电话

来源：`test - 5.txt`

ActivityA 打开 ActivityB 获取联系人电话。（20 分）

ActivityA 的布局里有一个 TextView（id:`tv1`）和 Button（id:`btn`，onClick:`getPhone`）。点击按钮打开 ActivityB，去获取联系人电话，将获取到的电话显示在 TextView 中。

```java
public class ActivityA extends AppCompatActivity {

    // 1.

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_a);
        // 2.
    }

    // 3.
    public void getPhone(View view) {

    }

    @Override
    protected void onActivityResult(int requestCode, int resultCode, @Nullable Intent data) {
        super.onActivityResult(requestCode, resultCode, data);
        // 4.
    }
}
```

ActivityB 的布局里有一个 EditText（id:`et1`）和一个 Button（id:`btn`，onClick:`retPhone`）。点击按钮获取用户输入的电话，并将电话返回。

```java
public class ActivityB extends AppCompatActivity {

    // 5.

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_b);

        // 6.
    }

    // 7.
    public void retPhone(View view) {

    }
}
```

注意：编号处不一定需要填写代码。

### 作业 6：POST 请求与 JSON 解析

来源：`test - 6.txt`

#### 1. 使用 POST 请求获取字符串

写出使用 Post 请求获取指定 url 返回的字符串，需要传递的参数为：`username` 和 `password`。（10 分）

```java
String httpPost(String url,String name,String pwd){

}
```

函数的调用格式为：

```java
new Thread(){
    public void run(){
        String str=httpPost(url,name,pwd);
        ......
    }
}.start();
```

#### 2. 数据解析

假设获取的 JSON 字符串如下：

```json
{
    "data": [
        {
            "desc": "扔物线",
            "id": 29,
            "title": "View 嵌套太深会卡？来用 Jetpack Compose，随便套——Compose 的 Intrinsic Measurement",
            "url": "https://www.bilibili.com/video/BV1ZA41137gr"
        },
        {
            "...": "..."
        },
        {
            "...": "..."
        },
        {
            "...": "..."
        }
    ],
    "errorCode": 0,
    "errorMsg": ""
}
```

说明：`errorCode` 为 0，代表成功；否则为错误。

实体类 `Article` 用来存储数据：

```java
public class Article{
    private String desc;
    private int id;
    private String title;
    private String url;
    ...... // 省略构造函数及 get、set 方法
}
```

完成以下方法，实现解析 JSON 数据功能，参数为获取到的 JSON 字符串。（10 分）

```java
List<Article> parseArticle(String strJson){

}
```

### 作业 7：ContentObserver 与 Fragment 动态加载

来源：`test - 7.txt`

#### 1. 注册内容观察者

已知内容提供者的授权为：`content://cn.zzuli.mycp`。当使用该内容提供者添加一条记录时，会发送一个数据改变到消息中心。请注册一个内容观察者，监听到此编号后将变化的数据获取到，并显示到 id 为 `tv1` 的 TextView 内。

```java
public class MainActivity extends AppCompatActivity {
    private TextView tv1;
    private ContentResolver resolver;
    private MyObserver observer;

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        EdgeToEdge.enable(this);
        setContentView(R.layout.activity_main);
        ViewCompat.setOnApplyWindowInsetsListener(findViewById(R.id.main), (v, insets) -> {
            Insets systemBars = insets.getInsets(WindowInsetsCompat.Type.systemBars());
            v.setPadding(systemBars.left, systemBars.top, systemBars.right, systemBars.bottom);
            return insets;
        });

        // 1.（5分）
    }

    @Override
    protected void onDestroy() {
        // 2.（2分）
        super.onDestroy();
    }

    class MyObserver extends ContentObserver{
        public MyObserver(Handler handler) {
            super(handler);
        }

        @Override
        public void onChange(boolean selfChange, @Nullable Uri uri) {
            // 3.（8分）
        }
    }
}
```

对应的表结构如下：

```text
_id integer
name String
age integer
```

#### 2. 动态加载 Fragment

已知 ActivityA 里添加了一个 FragmentContainerView（id:`fcv`），请写出动态加载 MyFragment 的代码。（5 分）

### 作业 8：Service 与 BroadcastReceiver

来源：`test - 8.txt`

已知服务的代码如下：

```java
public class MyService extends Service {
    public MyService() {
    }

    public class MyBinder extends Binder {
        // 1. 返回Service实例，供Activity调用
        public MyService getService() {

        }
    }

    @Override
    public IBinder onBind(Intent intent) {

    }

    @Override
    public int onStartCommand(Intent intent, int flags, int startId) {
        // 2. 服务启动时直接播放

        return super.onStartCommand(intent, flags, startId);
    }

    public void play(){
        // 3.发送广播，并携带字符串参数：zzuli（3分）
    }
}
```

在 ActivityA 中，实现如下功能：

启动服务 MyService。（3 分）

```java
// 4.
```

调用或者执行服务里的 `play` 方法。如需要修改服务，只需要带上编号完成代码即可。（10 分）

```java
// 5.
```

在 ActivityA 内定义了一个广播接收者，接收 `play` 方法发送的广播，并将接收的数据显示在 id 为 `tv1` 的 TextView 内。

```java
class MyReceiver extends BroadcastReceiver {
    @Override
    public void onReceive(Context context, Intent intent) {
        // 6.（4分）
    }
}
```

### 作业 9：数据库操作

来源：`test -9没有让提交.txt`

#### 1. 创建数据库

```java
public class MyHelper extends SQLiteOpenHelper {

    public MyHelper(@Nullable Context context, @Nullable String name,
                    @Nullable SQLiteDatabase.CursorFactory factory, int version) {
        super(context, name, factory, version);
    }

    @Override
    public void onCreate(SQLiteDatabase db) {

    }

    @Override
    public void onUpgrade(SQLiteDatabase db, int oldVersion, int newVersion) {

    }
}
```

1. 若要创建一个 `user.db` 的数据库，版本为 1，请给出核心代码。（2 分）
2. 为数据库添加数据表：`user`，包括 `name` 和 `password` 列。（4 分）
3. 数据库运行一段时间后，需要为 `user` 表添加一个列：`state`，数据库中原有数据不能丢失，如何实现？（4 分）

#### 2. 使用 user 表进行用户验证

登陆窗口有两个 EditText，id 分别为：`et_name` 和 `et_pwd`，请给出使用 `user` 表进行用户验证的代码。（10 分）

```java
public void login(View v){

}
```

## 三、实验题

### 实验 1：Android 应用的 UI 编程

来源：`1.doc`

实验内容及要求：

1. 实现“注册”界面。
2. 点击提交时，使用 Toast 显示用户输入的信息。
3. 使用样式和主题优化实验题 1 的布局。
4. 使用 `strings.xml` 文件实现界面的国际化，并在手机上测试国际化的效果。
5. 使用列表控件实现购物商城。
6. 选做：实现图示界面。原文图片包含圆角文本框、按钮样式相关内容，可参考圆角文本框和 MaterialButton 的实现。

实验目的：

1. 掌握布局文件的创建。
2. 掌握基本 Android 控件的使用。
3. 掌握样式和主题的使用。
4. 掌握程序国际化的方法。
5. 掌握列表控件的使用。

### 实验 2：Activity

来源：`2.doc`

实验内容及要求：

1. 实现首页 Activity，点击“去桃园”按钮将打开桃园界面。
2. 在桃园界面，点击桃子可以将桃子摘下，并显示摘了多少桃子。
3. 点击“退出桃园”按钮，即可返回首页，并显示摘桃数量。
4. 仿美团菜单：在一个 Activity 中展示两个 Fragment，并实现 Activity 与 Fragment 之间的通信功能。

实验目的：

1. 掌握 Activity 的创建及其生命周期。
2. 掌握 Activity 的四种启动模式的区别。
3. 掌握 Activity 中的数据传递。
4. 掌握 Fragment 在 Activity 中的静态和动态用法。
5. 熟悉对 Fragment 的正确使用方式。
6. 理解 Fragment 的工作原理。

### 实验 3：Android 数据存储

来源：`3.doc`

实验内容及要求：

1. 实现用户登录 Activity，输入账号和密码后，点击“登录”按钮，实现如下功能：
2. 验证账号和密码是否正确。账号：`admin`，密码：`123456`。
3. 使用 SharedPreferences 保存正确的账号和密码。
4. 打开 MainActivity。
5. 再次打开登录界面，从 SharedPreferences 中读取保存的账号和密码并显示。
6. 将上题的 SharedPreferences 替换为使用 File，实现相同的功能。
7. 创建 SQLite 数据库，存储通讯录信息。
8. 创建通讯录 Activity，实现通讯录的添加、查询、修改和删除功能。
9. 使用 ListView 展示题 3 数据库中的通讯录数据。

实验目的：

1. 掌握 SharedPreferences 的保存与读取的方法。
2. 掌握 File 的保存与读取的方法。
3. 掌握 SQLite 数据库的创建，及数据库中数据的常用操作。
4. 掌握 ListView 的使用方法。

### 实验 4：Service 与 BroadcastReceiver

来源：`4.doc`

实验内容及要求：

#### 1. MediaStore 获取音乐列表

调用系统内容提供者 `MediaStore`，获取手机上的音乐文件，并使用列表控件显示歌曲信息。

已知：

```java
Uri: MediaStore.Audio.Media.EXTERNAL_CONTENT_URI
```

包含如下数据：

```java
歌曲ID: MediaStore.Audio.Media._ID
歌曲的名称: MediaStore.Audio.Media.TITLE
歌曲的专辑名: MediaStore.Audio.Media.ALBUM
歌曲的歌手名: MediaStore.Audio.Media.ARTIST
歌曲文件的路径: MediaStore.Audio.Media.DATA
歌曲的总播放时长: MediaStore.Audio.Media.DURATION
歌曲文件的大小: MediaStore.Audio.Media.SIZE
```

请实现歌曲列表。

说明：若使用模拟器，可以使用模拟器下载歌曲，或者将电脑上的歌曲拖动到模拟器即可完成拷贝。建议使用自己的手机进行调试，效果更佳。

#### 2. 使用 Service 播放歌曲

使用 Services 实现歌曲文件的播放。在第 1 题的基础上实现，步骤如下：

1. 添加下方的音乐播放控制。
2. 为列表控件添加事件，在单击歌曲时，使用 Service 播放点击的歌曲。
3. 播放歌曲时将播放图标更改为暂停图标。
4. 实现歌曲的播放、暂停、上一首、下一首功能。

#### 3. 使用广播更新界面信息

使用广播更新图 1 中的 `Title` 和 `Singer`，要求如下：

1. 在播放歌曲或者歌曲切换时，发送广播。
2. 在 MainActivity 中定义广播接收者，接收 Services 发送的广播，然后更新歌名和歌手信息。

实验目的：

1. 掌握服务的运行机制。
2. 掌握自定义广播接收者，并接收广播。
3. 掌握常用系统广播的接收。

### 实验 5：网络编程

来源：`5.doc`

实验内容及要求：

登录玩安卓（`www.wanandroid.com`），打开主界面，下载并显示文章列表。

#### 1. 实现登录功能

使用实验一实现的登录布局。

已知网络地址如下：

```text
https://www.wanandroid.com/user/login
```

方法：POST

参数：`username`、`password`

登录成功，服务器端返回 JSON 串，如：

```json
{"data":{"admin":false,"chapterTops":[],"coinCount":658,"collectIds":[17666],"email":"","icon":"","id":90114,"nickname":"android2021","password":"","publicName":"android2021","token":"","type":0,"username":"android2021"},"errorCode":0,"errorMsg":""}
```

请从该地址获取 JSON 数据，并解析。

其中：`errorCode` 为 0 代表登录成功；登录失败，可以从 `errorMsg` 中获取错误信息。

说明：用户名和密码可以使用：`ydrj2024`、`jsjyrgznxy`。登录界面仅为参考，可以自己设计一个。

#### 2. 获取并显示文章列表

从以下地址获取文章列表，并显示：

```text
https://www.wanandroid.com/article/list/0/json
```

#### 3. 选作：获取并显示收藏列表

接口如下：

```text
https://www.wanandroid.com/lg/collect/list/0/json
```

方法：GET

参数：页码拼接在链接中，从 0 开始。

该接口支持传入 `page_size` 控制分页数量，取值为 `[1-40]`。不传则使用默认值。一旦传入了 `page_size`，后续该接口分页都需要带上，否则会造成分页读取错误。

在网站上登录后，可以直接访问以下地址查看自己收藏的文章：

```text
https://www.wanandroid.com/lg/collect/list/0/json
```

说明：第 3 题为选作，如果不做，本实验成绩最高为 85 分。

实验目的：

1. 掌握使用 HttpUrlConnection 进行网络访问。
2. 掌握 Android 端文件的上传与下载。
3. 掌握使用当前主流的第三方 Jar 包进行网络访问。
