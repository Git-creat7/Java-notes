+++
date = '2026-04-07'
draft = false
title = '09-数据结构'
tags = []
categories = ["数据结构"]
+++
# 回顾
## Malloc
`malloc` 的全称是 **Memory Allocation**（内存分配）。它是 C 语言中从**堆（Heap）**区域手动申请内存的核心函数
```C
	void* malloc(size_t size);
```
- **参数 `size`**：想要申请的内存字节数。通常配合 `sizeof` 使用，以确保跨平台兼容性。
    
- **返回值**：
    
    - 成功：返回分配到的内存**首地址**（`void*` 类型，可以强转为任何类型）。
    - 失败：返回 `NULL`

| **特性**  | **栈 (Stack)** | **堆 (Heap) / malloc** |
| ------- | ------------- | --------------------- |
| **大小**  | 较小（通常几 MB）    | 很大（取决于系统内存）           |
| **控制权** | 系统自动分配/释放     | 程序员手动分配/释放            |
| **灵活性** | 固定大小，编译时确定    | **动态确定**，运行中可变        |
## 取地址
使用取地址符 & 获取数组的地址时，返回的是数组第 0个元素的内存地址
```C
	int a[] = {1,2,3,4,5};
	cout << &a << endl;//0x43a03ff9c0
	cout << &a[0] << endl;//0x43a03ff9c0
```
## 指针与结构体
```C++
// 用 typedef 定义别名：Student 是结构体类型，*PStu 是结构体指针类型
typedef struct {
    int id;
    int age;
} Student, *PStu;

int main() {
    Student s1 = {101, 20};
    
    // 定义指针的两种等价写法
    Student *p1 = &s1;  // 常规写法
    PStu p2 = &s1;      // 更加简洁的别名写法
    
    // 统一使用 -> 访问
    p2->id = 102;
    p2->age = 21;
    printf("ID: %d, 年龄: %d\n", p1->id, p1->age);

    return 0;
}
```
# 线性表
由 n(n>0)个数据特性相同的元素构成的有限序列，称为线性表。
- 线性表是 n 个数据元素的有限序列，其中 n个数据是相同数据类型的
- 线性表中元素的个数 n(n≥0)定义为线性表的长度，当 n=0 时称之为空表。
- 对于非空的线性表或线性结构，其特点是:
	- **存在唯一的一个被称作“第一个”的数据元素**
	- **存在唯一的一个被称作“最后一个”的数据元素**
	- **除第一个元素外，结构中的每个数据元素均只有一个前驱**
	- **除最后一个元素外，结构中的每个数据元素均只有一个后继**

## 顺序表
用一组连续的内存单元依次存储线性表的各个元素，也就是说，逻辑上相邻的元素，实际的物理存储空间也是连续的。
### 初始化顺序表
```C
#include <stdio.h>
#define MAXSIZE 100
typedef int ElemType;
typedef struct {
    ElemType data[MAXSIZE];
    int length;
}SeqList;
void initList(SeqList *L){
    L->length = 0;
}
int main(void){
    SeqList L; initList(&L);
    printf("初始化成功，目前长度占用：%d\n", L.length);
    printf("目前占用内存大小：%d\n", sizeof(L.data));
    return 0;
}
```
### 在尾部添加元素
```C
int appendElem(SeqList *L,ElemType e){ //尾插法
    if(L->length == MAXSIZE) {
        printf("顺序表已满");
        return 0;
    }  
    L -> data[L -> length] = e;
    L -> length ++;
    return 1;
}
```
### 插入元素
`时间复杂度 O(1) ~ O(n)`
```C
int insertElem(SeqList *L, int pos, ElemType e){ //pos：位置
    if(L->length == MAXSIZE){
        printf("顺序表已满");
        return 0;
    }
    if(pos < 1 || pos > L->length + 1){
        printf("位置不合法");
        return 0;
    }
    for(int i = L->length; i >= pos; i--){
        L->data[i] = L->data[i-1];
    }
    L->data[pos-1] = e;
    L->length++;
    return 1;
}
```
### 删除元素
```C
int deleteElem(SeqList *L,int pos,ElemType *e){
    if(L->length == 0 || pos < 1 || pos > L->length) 
    return 0;
    //e = 地址 *e = 值   
    *e = L -> data[pos-1];
    for(int i = pos; i < L->length;i++)
        L->data[i-1] = L -> data[i];
    L->length --;
    return 1;
}
```

### 查找元素
```C
int findElem(SeqList *L,ElemType e){
    for(int i = 0; i < L->length; i++){
        if(L -> data[i] == e){
            return i+1;
        }
    }
    return 0;
}
```
### 动态分配内存地址初始化
```C
typedef struct {
	ElemType *data;
	int length;
}SeqList;
SeqList* initList(){
    SeqList *L = (SeqList*)malloc(sizeof(SeqList));
    L -> data = (ElemType*)malloc(sizeof(ElemType)*MAXSIZE);
    L -> length = 0;
    return L;
}
```
- **第一次 `malloc(sizeof(SeqList))`**：在堆上开辟了一块空间，用来存放 `data` 指针、`length` 等成员。
	    
- **第二次 `malloc(sizeof(ElemType)*MAXSIZE)`**：在堆上开辟了一块连续的数组空间，并将它的首地址赋给 `p->data`
- 这样就可以传入list了：
- ```C
	int main(void){
	SeqList* list = initList();
	appendElem(list,32);
	listElem(list);
	return 0;
	}
   ```


## 链表
- 链表中的结点是独立的对象，在内存中是不连续的，每个节点包含数据值和下一个结点的地址
- 链表==查询慢==，无论查询哪个数据都从头开始找。
- 链表的==增删快==（对比数组）
### 节点
在链表中，数据不再是连续存放的，而是分散在内存的各个角落。每个数据块被称为一个**节点**。
一个节点由两部分组成：
1. **数据域**：存想存的东西（如 `int`）。
    
2. **指针域**：存**下一个节点**的地址（那个“钩锁”）
```C
Node* initList(){
    Node* head = (Node*)malloc(sizeof(Node));
    head -> data = 0;
    head -> next = NULL;
    return head;
}
```
别名：增加可读性
```C
typedef struct Node {
    ElemType data;
    struct Node *next;
} Node, *LinkedList; // LinkedList 等价于 Node*
```
如果这样写了，`main` 函数里就可以写成： `LinkedList list = initList();` 
这样从字面上看，`list` 就代表了“整个链表”，而不仅仅是一个节点指针了。
**但在底层，它依然是一个 `Node*`**

---
### 头插法
为了保证链表不断裂，这两行代码的**顺序**是绝对不能错的：

1. `p -> next = L -> next;`
    
    **逻辑**：让新节点 `p` 先牵住原先排在第一位的那个人。如果后面原本没节点（`NULL`），那么 `p->next` 就指向 `NULL`。
    
2. `L -> next = p;`
    
    **逻辑**：让头结点 `L` 改手牵住新节点 `p`
```C
int insertHead(Node* L,ElemType e){
	Node* p = (Node*)malloc(sizeof(Node));
	p -> data = e;
	p -> next = L -> next;
	L -> next = p;
}
```
假设依次插入 `10`, `20`, `30`：
1. 插入 10：`Head -> 10 -> NULL`
    
2. 插入 20：`Head -> 20 -> 10 -> NULL`
    
3. 插入 30：`Head -> 30 -> 20 -> 10 -> NULL`
#### 无头结点的头插法 
```C
// 无头结点头插法，需要修改头指针，所以用二级指针
int insertHead(Node** L, ElemType e)
{
    Node* p = (Node*)malloc(sizeof(Node));
    if(p == NULL) return 0;
    p->data = e;

    p->next = *L;   // 新结点接原链表开头
    *L = p;         // 头指针更新为新结点
    return 1;
}
```
**无二级指针**
```C
// 传入旧头指针，返回插入后的新头指针
Node* insertHead(Node* L, ElemType e)
{
    // 新建结点
    Node* p = (Node*)malloc(sizeof(Node));
    p->data = e;

    // 头插核心
    p->next = L;   // 新结点指向原第一个结点
    L = p;         // 头指针更新为新结点

    return L;      // 返回新头结点
}
```

---

### 尾插法
要实现尾插，最关键的是找到当前链表的**最后一个节点**。

1. 创建一个指针 `r`（取名为 rear，意为尾部），初始指向头结点。
    
2. 通过 `while` 循环让 `r` 走到尽头（即 `r->next == NULL` 的地方）。
    
3. 把新节点挂在 `r` 的后面
```C
int insertTail(LinkList L,ElemType e){
    //创建新节点
    LinkList h = (LinkList)malloc(sizeof(Node));
    if(h == NULL) return 0;
    h -> data = e;
    h - > next = NULL;
    //找到尾节点
    Node* r = L;
    while(r -> next != NULL){
        r = r -> next;
    }
    //将新节点插入到尾节点后面
    r -> next = h;
    return 0;
}
```
假设链表现在是 `Head -> 10 -> NULL`，要插入 `20`：

- **找尾巴**：`r` 发现 `r->next` 指向 10，于是 `r` 变成 10。10 的 `next` 是 `NULL`，循环停止，`r` 现在就是 10。
    
- **挂载**：执行 `r->next = p`，即 10 的 `next` 指向了 20。
    
- **结果**：`Head -> 10 -> 20 -> NULL`
#### 初始化尾插法
```C

```

---

### 在指定位置插入
假设要将新元素 `e` 插入到第 `i` 个位置（从 1 开始计数）：
1. 找到第 `i-1` 个节点（即**目标位置的前驱节点**）。
    
2. 创建一个新节点 `s`。
    
3. 将 `s` 的 `next` 指向原先的第 `i` 个节点。
    
4. 将第 `i-1` 个节点的 `next` 指向 `s`
```C
int insertPos(LinkList L,ElemType e,int pos){
	 Node* m = L;//当前扫描到的节点
	 int j = 0 // 当前p指向的是第几个节点
	
	 while(m != NULL && j < pos - 1){
	     m = m->next;
	     j++;
	 }
	 if(m == NULL || pos < 1) return 0;
	 Node* p = (Node*)malloc(sizeof(Node));
	 if(p == NULL) return 0;
	 p -> data = e;
	 p -> next = m -> next;
	 m -> next = p;
	 return 1;
}
```
### 删除节点
假设要删除第 `i` 个节点：

1. 找到第 `i-1` 个节点（**前驱节点**），记为 `p`。
    
2. 定义一个临时指针 `q` 作为要删除的节点（即 `p->next`）。
    
3. 修改指针：让 `p->next` 指向 `q->next`（跳过被删除节点）。
    
4. 释放 `q` 节点的内存
```C
int deletePos(LinkList L,int pos,ElemType *e){
    Node* p = L;//当前扫描到的节点
    int j = 0; // 当前p指向的是第几个节点
    while(p -> next != NULL && j < pos - 1){
        p = p -> next;
        j++;
    }
    if(p -> next == NULL || pos < 1) return 0;

    Node* q = p -> next;//q作为被删除的节点
    *e = q -> data;//将被删除节点的数据保存到e中
    p -> next = q -> next;//将q的下一个节点链接到p上
    free(q);
    return 1;
}
```
### 释放链表
假设链表结构为：`L (头结点) -> A -> B -> C -> NULL`

- **`Node* p = L -> next;`**
    
    - **作用**：让指针 `p` 指向链表的第一个真正存放数据的节点（首元节点 `A`）。
        
    - **注意**：这里跳过了头结点 `L`，因为头结点通常不存数据，只需清空它后面的内容。
        
- **`Node* q;`**
    
    - **作用**：声明一个辅助指针 `q`。它是实现逻辑的关键，用来“探路”。
        
- **`while(p != NULL){ ... }`**
    
    - **作用**：只要还没走到链表末尾，就继续循环执行释放操作。
        
- **核心循环体内部：**
    
    1. **`q = p -> next;`**：在杀掉 `p` 之前，先让 `q` 指向 `p` 的下一个节点（比如 `B`）。**如果不这样做，一旦释放了 `p`，就再也找不到 `B` 了。**
        
    2. **`free(p);`**：释放当前节点 `p` 所占用的内存。
        
    3. **`p = q;`**：让 `p` 追上 `q` 的位置，准备处理下一个节点。
        
- **`L -> next = NULL;`**
    
    - **作用**：当所有数据节点（A, B, C）都释放完后，头结点 `L` 依然存在，但它现在指向的是已经被销毁的内存。
        
    - **目的**：这一步将头结点的指针域置为空。此时，链表在逻辑上变回了一个**空的带头结点链表**

```c
void freeList(LinkList L){
   if(L == NULL) return;
   Node* p = L -> next;
   Node* q;
   while(p != NULL){
       q = p -> next;
       free(p);
       p = q;
   }
   L -> next = NULL;
}
```
### 例题
![](09-数据结构-2.png)
```C
#include <bits/stdc++.h>

typedef char ElemType;
typedef struct Node {
    ElemType data;
    struct Node *next;
} Node, *LinkList;

// 尾插法：创建一个带头结点的链表
LinkList createList_Tail(const char *s) {
    LinkList L = (LinkList)malloc(sizeof(Node));
    L->next = NULL;
    Node *r = L;
    for (int i = 0; s[i] != '\0'; i++) {
        Node *p = (Node *)malloc(sizeof(Node));
        p->data = s[i];
        r->next = p;
        r = p;
    }
    r->next = NULL;
    return L;
}

Node* findIntersectionNode(LinkList headA, LinkList headB) {
    // 安全检查
    if (headA == NULL || headB == NULL || headA->next == NULL || headB->next == NULL) return NULL;

    int lenA = 0, lenB = 0;
    // 从第一个数据节点开始计算长度
    Node* pA = headA->next;
    Node* pB = headB->next;

    while (pA != NULL) { lenA++; pA = pA->next; }
    while (pB != NULL) { lenB++; pB = pB->next; }

    int step = abs(lenA - lenB);
    
    // 重新指向首元节点
    pA = headA->next;
    pB = headB->next;

    // 让长链表先走
    if (lenA > lenB) {
        while (step--) pA = pA->next;
    } else {
        while (step--) pB = pB->next;
    }
    // 同步推进寻址
    while (pA != pB) {
        pA = pA->next;
        pB = pB->next;
    }
    return pA; // 如果不相交，最后 pA, pB 都会是 NULL，返回 NULL 也是正确的
}

int main(void) {
    // 1. 创建公共后缀
    LinkList common = createList_Tail("ing");
    
    // 2. 创建各自的前缀
    LinkList being = createList_Tail("be");
    LinkList loading = createList_Tail("load");

    // 3. 构造相交结构：将前缀末尾指向公共部分的第一个数据节点
    Node *p = being;
    while (p->next != NULL) p = p->next;
    p->next = common->next; 

    Node *q = loading;
    while (q->next != NULL) q = q->next;
    q->next = common->next;

    // 4. 调用函数
    Node* result = findIntersectionNode(being, loading);
    
    if (result != NULL) {
        printf("相交节点数据为: %c\n", result->data); // 输出 i
    } else {
        printf("未发现相交节点\n");
    }

    return 0;
}
```

---

### 链表的应用
#### 删除中间节点：快慢指针
##### 有头链表：
```C
LinkList deleteMiddle(LinkList head){
    //如果所给的链表为空，直接返回
    if(head==NULL||head->next==NULL) return NULL;
    Node* fast = head->next;
    Node* slow = head;
    while(fast!=NULL && fast->next!=NULL){
        fast = fast->next->next;
        slow = slow->next;
    }
    //slow指向中间的前驱节点
    Node* temp = slow->next;
    slow->next = temp->next;
    free(temp);
    return head;
}
```
##### 无头链表：
```C
LinkList deleteMiddle2(LinkList head){
    if(head==NULL)return NULL;
    Node* fast = head;
    Node* slow = head;
    Node* pre = NULL;
    while(fast!=NULL && fast->next!=NULL){
        fast = fast->next->next;
        pre = slow;
        slow = slow->next;
    }
    //pre指向前驱节点
    if(pre!=NULL){
        pre->next = slow->next;
        free(slow);
    }
    return head;
}
```
##### 兼容两种链表
```C
LinkList deleteMiddle(LinkList head){
    Node* dummy = (Node*)malloc(sizeof(Node));
    dummy->next = head;

    Node* fast = dummy->next;
    Node* slow = dummy;
    while(fast!=NULL && fast->next!=NULL){
        fast = fast->next->next;
        slow = slow->next;
    }
    //slow指向中间的前驱节点
    Node* temp = slow->next;
    slow->next = temp->next;
    free(temp);
    head = dummy->next;
    free(dummy);
    return head;
} 
```
#### hot 反转链表：指针迭代法
```C
LinkList reverse(LinkList head){
	if (head == NULL || head->Next == NULL) return L;

    Node* pre = NULL;
    Node* curr = head->NULL;
    while(curr!=NULL){
        Node* next = curr->next; // 保存下一个节点
        curr->next = pre;        // 反转指针
        pre = curr;             // pre 往前走
        curr = next;            // curr 往前走
    }
    return pre;                 // 新的头结点
}
```

#### hot 合并两个有序链表：三指针法
```C
List Merge(List L1,List L2){

    List dummy = (List)malloc(sizeof(struct Node));
    dummy->Next = NULL;
    List list1 = L1->Next;
    List list2 = L2->Next;
    List curr = dummy;
    
    while(list1!=NULL && list2!=NULL){
        if(list1->Data >= list2->Data){
            curr->Next  = list2;
            curr = list2;
            list2 = list2->Next;
        }else{
            curr->Next = list1;
            curr = list1;
            list1 = list1->Next;
        }
    }
    curr->Next = (list1 != NULL) ? list1 : list2;
    return dummy->next;
}
```
#### hot K个一组逆转链表


---

### 链表的局限
| **局限性类别** | **具体表现描述 **                    | **常见的改进/替代方案 **               |
| --------- | ------------------------------ | ----------------------------- |
| **访问速度**  | 无法随机访问，查找第 $n$ 个节点需要 $O(n)$ 时间 | **数组**（随机访问）或 **跳表**（通过索引层加速） |
| **遍历方向**  | 只能向后走，无法直接获取“前驱”节点             | **双链表**（增加 `prev` 指针）         |
| **插入/删除** | 在已知节点前操作或删除当前节点时，仍需从头遍历找前驱     | **双链表** 或 **使用虚拟头节点**（简化边界操作） |
| **空间开销**  | 每个数据都需要额外存储一个指针空间              | **数组** 或 **静态链表**（在连续空间内模拟链表） |
| **可靠性**   | 链表一旦中间断裂（指针指向错误），后续数据全部丢失      | **双链表**（具备一定的冗余恢复能力）          |

---
## 循环链表
- 循环链表（Circular Linked List）是单链表的一个奇妙变体。
- 它最显著的特征是：**最后一个节点的指针不再指向 `NULL`，而是重新指向头节点**，从而形成一个闭合的环
- 在单链表中，我们判断遍历结束的条件是 `curr->next == NULL`；而在循环链表中，由于没有“尽头”，我们通常判断 `curr->next == head`

|**优势**|**描述**|**实际应用场景**|
|---|---|---|
|**首尾直达**|如果持有尾指针，可以在 $O(1)$ 时间找到头和尾。|队列的循环实现、缓冲区管理。|
|**无死角遍历**|从链表中任何一个节点出发，都可以访问到其他所有节点。|播放器的循环播放列表、多轮巡检任务。|
|**简化合并**|合并两个循环链表只需修改两个指针，无需遍历。|资源池的动态合并。|
### 找出环的起始节点
```C
LinkList isCycleLink(LinkList head){
    Node* fast = head;
    Node* slow = head;
    while (fast!=NULL && fast->next!=NULL){
            fast = fast->next->next;
            slow = slow->next;
            
            if(fast==slow){
	            //第三节点从头出发
                Node* y = head;
                while(y!=slow){
                    y=y->next;
                    slow=slow->next;
                }
                return y; // 返回入环节点
            }
                
    }
    return NULL;
}
//有头将初始化head改为head->next即可
```

---
## 双向链表
- 双向链表是链表的“完全体”。
- 它通过增加一个空间开销，彻底解决了单链表“无法回头”的痛点。
### 优势
- **真正的 $O(1)$ 删除**：
    
    在单链表中，如果要删除节点 `p`，必须从头遍历找到 `p` 的前驱。但在双向链表中，`p->prev` 直接就告诉了前驱是谁。
    
- **双向遍历**：
    
    你可以从头走到尾，也可以从尾走到头。这在处理某些需要倒序查看数据的算法（如最近最少使用缓存 LRU）时是刚需。
    
- **插入更灵活**：
    
    你可以在某个节点的前面或后面直接插入，而不需要像单链表那样总是要在“前一个节点”上做文章。
### 头插法

```C
LinkList insertHead(LinkList head,ElemType data){
    Node* node = (Node*)malloc(sizeof(Node));
    if(node == NULL) return head;
    node->data = data;
    node->next = head->next;
    if(head->next != NULL){
        head->next->prev = node;
    }
    node->prev = head;
    head->next = node;
    return head;
}
```
### 尾插法
```C
LinkList insertTail(LinkList head,Node* oldTail,ElemType data){
    Node* tail = (Node*)malloc(sizeof(Node));
    if(tail==NULL) return NULL;
    tail->data = data;
    tail->prev = oldTail;
    oldTail->next = tail;
    tail->next = NULL;
    return head;
}	
```
### 在指定位置插入
```C
LinkList insertPos (LinkList head,int pos,ElemType data){
    Node* p = head;
    int j = 0;
    //寻找第pos-1的节点
    while(p != NULL && j <pos-1){
        p = p->next;
        j++;
    }
    //超过链表长度
    if(p==NULL) return NULL;
    Node* node = (Node*)malloc(sizeof(Node));
    if(node==NULL) return NULL;
    node->data = data;

    // p     ->     pos 
    //      node
    node->next = p->next;
    p->next = node;
    p->next->prev = node;
    node->prev = p;
    return head;
}
```
### 删除节点
```C
// 双向链表：删除第 pos 个节点
// 参数：head 链表头结点，pos 删除位置，data 带回被删节点的值
// 返回：删除成功返回头结点，失败返回 NULL
LinkList deletePos(LinkList head, int pos, ElemType* data) {
    // 位置非法（小于1），直接返回
    if (pos < 1)
        return NULL;

    // p 指向第一个数据节点（带头结点）
    Node* p = head->next;
    int j = 1;  // j 记录当前节点的位置

    // 找到第 pos 个节点：p 向后走，直到 j == pos
    while (p != NULL && j < pos) {
        p = p->next;
        j++;
    }

    // 循环结束后 p == NULL，说明 pos 超出链表长度，删除失败
    if (p == NULL)
        return NULL;
    // 把被删除节点的数据保存到 *data 中带回
    *data = p->data;

    // 核心：双向链表删除节点
    // 1. 让前驱节点的 next 直接指向后继节点
    p->prev->next = p->next;

    // 2. 如果不是最后一个节点，让后继节点的 prev 指向前驱节点
    if (p->next != NULL) {
        p->next->prev = p->prev;
    }
    // 释放被删除节点的空间
    free(p);

    // 删除成功，返回链表头结点
    return head;
}

```
---


 

# 栈Stack
- **后进先出，先进后出**
- 压栈/进栈：数据进入栈模型的过程
- 弹栈/出栈：数据离开栈模型的过程

# 队列Queue
- **先进先出，后进后出**
- 入队列：数据从**后端进入**队列模型的过程
- 出队列：数据从**前端离开**队列模型的过程

# 数组
- ==查询速度快==：查询数据通过地址值和索引定位，查询任意数据耗时相同（元素在内存中是连续存储的）
- ==删除效率低==：要将原始数据删除，同时后面每个数据前移
- ==添加效率低==：添加位置后的每个数据后移，再添加元素

# 表达式求值
## 






# 树
```Java
	 [父节点地址值，值，左子节点地址值，右子节点地址值]
	 度：每一个节点的 子节点 数量
	 树高：树的总层数
	 
	 
	   10        <-- 根节点
       /  \
      5    15     <-- 第二层
     / \     \
    3   7    20   <-- 第三层
       /
      6           <-- 第四层
```

## 二叉树的遍历方式
### 前序遍历

- **顺序：根节点 -> 左子树 -> 右子树**
    
- **描述：** 只要遇到节点，先访问它，然后递归地去处理它的左子树，最后处理右子树。
    
- **特点：** 根节点总是第一个被访问的。
    

### 中序遍历 

- **顺序：左子树 -> 根节点 -> 右子树**
    
- **修正：** 并不是简单地“从最左边开始”，而是**先完整地遍历左子树**，再访问当前节点，最后遍历右子树。
    
- **特点：** 对于**二叉搜索树 (BST)**，中序遍历的结果是一个严格递增的序列。
    

### 3. 后序遍历

- **顺序：左子树 -> 右子树 -> 根节点**
    
- **修正：** 它的逻辑是**先完成左右子树的访问**，最后才回到当前节点。
    
- **特点：** 根节点总是最后一个被访问的。常用于文件目录的大小计算或删除整棵树。
    

### 4. 层序遍历 

- **顺序：按层从上到下，每层从左到右**
    
- **描述：** 借助**队列 (Queue)** 实现。先将根节点入队，然后依次弹出节点，并将该节点的左右子节点入队。

|**遍历方式**|**访问顺序简记**|**核心逻辑**|
|---|---|---|
|**前序**|**根** - 左 - 右|遇到就拿，先根后后|
|**中序**|左 - **根** - 右|左边到底，中间插一杠|
|**后序**|左 - 右 - **根**|左右搞定，最后回根|
|**层序**|按层遍历|广度优先 (BFS)|

---


## 二叉查找树
- 每一个节点上最多有两个子节点
- 任意节点左子树上的值都小于当前节点
- 任意节点右子树上的值都大于当前节点
![](09.png)

### 添加节点
- 小的存左边，大的存右边，一样的不存
### 弊端
- 左子树全部为空，从形式上看，更像一个==单链表==
- 插入速度没有影响
- 查询速度明显降低
![](09-1.png)

### 解决弊端：平衡二叉树
平衡二叉树也叫 **平衡二叉搜索树（Self-balancing binary search tree）**，又被称为 AVL 树，可以保证 **查询效率较高**。它是解决 **二叉排序** 可能出现的查询问题。

 - 二叉查找树
 - 规则：==**任意**节点左右子树高度差不超过1==
![](09-3.png)

#### 保持平衡的旋转机制
- 左旋
- 右旋
- 当添加一个节点时，该树不再是一颗平衡二叉树 -> 旋转
#### 左旋
- 确定支点：从添加的节点开始，不断的往父节点找不平衡的节点

- 步骤：
	- 以不平衡的点作为支点
	- 把支点左旋降级，变成左子节点
	- 晋升 原来的右子节点

| ![500](09-4.png) | ![500](09-5.png) |
| ---------------- | ---------------- |


- 步骤：
	- 以不平衡的点作为支点
	- 将根节点的右侧往左拉
	- 原先的右子节点变成新的父节点，并把多余的左子节点出让，给已经降级的根节点当右子节点 

| ![500](09-6.png) | ![500](09-7.png) |
| ---------------- | ---------------- |


#### 需要旋转的四种情况
##### 左左LL
- 当==根结点左子树的左子树==有节点插入，导致二叉树不平衡
- 一次右旋
##### 左右LR
- 当==根结点左子树的右子树==有节点插入，导致二叉树不平衡
- 先局部左旋 -> 左左LL
- 再整体右旋
##### 右右RR
- 当==根结点右子树的右子树==有节点插入，导致二叉树不平衡
- 一次左旋
##### 右左RL
- 当==根结点右子树的左子树==有节点插入，导致二叉树不平衡
- 先局部右旋 -> 右右RR
- 再整体左旋

---

![](09-8.png)

---

## 红黑树
- 平衡二叉树的弊端：在添加节点的时候，旋转次数太多，造成添加节点的时间浪费
- 红黑树刚开始被称为**平衡二叉B树**
- 是一种特殊的二叉查找树，红黑树的每一个节点上都有存储位表示节点的颜色
- <span style="color: #dc3545;">每一个节点可以是红或者黑；红黑树不是高度平衡的，他的平衡是通过“红黑规则“进行实现的</span>
- 是一个二叉查找树
- ==但是不是高度平衡的==
- 条件：特有的<span style="color: #dc3545;">红黑规则</span>
### 红黑规则
1. 每一个节点是红色或黑色
2. 根节点必须是黑色的
3. 如果一个节点没有子节点或者父节点，则该节点相应的指针属性值为Nil，这些Nil被视为叶节点，每个叶节点 (Nil) 是黑色的
4. 如果某一个节点是红色的，那么它的子节点必须是黑色（不能出现两个红色节点相连的情况）
5. 对每一个节点，从该节点到其所有后代叶节点的简单路径上，均包含相同数目的黑色节点

#### 添加节点
- 添加的节点默认是红色的（红色效率高）
- 查找、插入、删除的时间复杂度最坏为O(log n)
![](09-10.png)

