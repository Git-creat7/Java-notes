+++
date = '2026-08-11'
draft = false
title = '消息队列'
tags = []
categories = ["Redis"]
+++

> 本文更新于 2026-08-11

基于阻塞队列的异步秒杀存在以下问题：
- 内存限制问题
- 数据安全问题

# 消息队列
- **消息队列（Message Queue，简称 MQ）** 是一种在分布式系统中用于**应用程序之间进行异步通信**的中间件
- **生产者（Producer）**、**消息队列（Message Queue）** 和 **消费者（Consumer）** 是分布式异步通信架构的核心三要素，本质上是典型的“产生数据-暂存数据-消费数据”模型

它们的关系就像**寄快递**：生产者是发件人，消息队列是快递暂存柜/中转站，消费者是收件人

- 安全
- 不受内存限制

## 三种不同的方式实现消息队列
### List
### PubSub 发布订阅
**优点**
- 采用发布订阅模型，支持多生产、多消费
**缺点**
- 不支持数据持久化
- 无法避免数据丢失
- 消息堆积有上限，超出时数据丢失
### stream类型消息队列
- 消息可回溯
- 一个消息可以被多个消费者读取
- 可以阻塞读取
- 有消息漏读的风险s
#### 消费者组
01
消息分流
队列中的消息会分流给组内的不同消费者，而不是重复消费，从而加快消息处理的
速度
02
消息标示
消费者组会维护一个标示，记录最后一个被处理的消息，哪怕消费者宕机重启，还会从标示之后读取消息。确保每一个消息都会被消费
03
消息确认
消费者获取消息后，消息处于pending状态，并存入一个pending-list。当处理完成后需要通过XACK来确认消息，标记消息为已处理，才会从pending一list移除。
#### 